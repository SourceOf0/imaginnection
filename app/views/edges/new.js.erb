
<% if @from_node.valid? %>
  
  <%# フォームを表示するためにハッシュを変える %>
  window.location.hash = "edge-new-" + encodeURIComponent("<%=j @from_node.name.html_safe %>");

<% else %>
  
  $("#info-modal").html("<%=j render 'layouts/error_messages', model: @from_node %>").modal("show");
  
<% end %>
