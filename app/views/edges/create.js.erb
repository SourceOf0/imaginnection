
<% if @from_node.valid? && @to_node.valid? %>

  <%# エッジを投稿する %>
  imaginnection.createEdge("<%= current_user.ref_id %>", "<%= @from_node.name %>", "<%= @to_node.name %>", <%= @is_hide_user %>);
  
  <%# エッジ一覧を表示するためハッシュを変える %>
  window.location.hash = "";

<% end %>
